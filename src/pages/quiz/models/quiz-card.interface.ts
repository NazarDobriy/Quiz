export interface CardTheme {
    titleClass: string;
    subtitleClass: string;
    iconSrc: string;
    backgroudClass: string;
}

export interface ICard {
    id?: number;
    title: string;
    subtitle: string;
}